[{"content":"","date":"15 August 2024","permalink":"/","section":"Blowfish","summary":"","title":"Blowfish"},{"content":"","date":"15 August 2024","permalink":"/tags/distributed-system/","section":"Tags","summary":"","title":"distributed system"},{"content":"","date":"15 August 2024","permalink":"/tags/docs/","section":"Tags","summary":"","title":"docs"},{"content":"开一个新坑（我就是这种喜欢开很多坑然后三分钟热度的人😭\nFrom MIT 6.5840 / Spring 2024, Lab1 - MapReduce\nMapReduce Paper: http://research.google.com/archive/mapreduce-osdi04.pdf\nPre Handle # 在 src/main/ 目录下，已经给出了sequential版本的实现: mrsequential.go(只需要运行该文件就可以完成sequential mapreduce).\n在这个目录下还有两个文件 mrcoordinator.go \u0026amp; mrworker.go，它们会调用 src/mr/ 目录下的 coordinator.go和worker.go文件中的函数来完成分片模拟下的mapreduce的处理。\n我们可以观察一下mrcoordinator.go 和 mrworker.go这两个文件：\n// mrcoordinator.go func main() { if len(os.Args) \u0026lt; 2 { fmt.Fprintf(os.Stderr, \u0026#34;Usage: mrcoordinator inputfiles...\\n\u0026#34;) os.Exit(1) } m := mr.MakeCoordinator(os.Args[1:], 10) for m.Done() == false { time.Sleep(time.Second) } time.Sleep(time.Second) } coordinator主要的执行逻辑就是初始化-\u0026gt;轮询直到任务执行结束\n//mrworker.go func main() { if len(os.Args) != 2 { fmt.Fprintf(os.Stderr, \u0026#34;Usage: mrworker xxx.so\\n\u0026#34;) os.Exit(1) } mapf, reducef := loadPlugin(os.Args[1]) mr.Worker(mapf, reducef) } // load the application Map and Reduce functions // from a plugin file, e.g. ../mrapps/wc.so func loadPlugin(filename string) (func(string, string) []mr.KeyValue, func(string, []string) string) { p, err := plugin.Open(filename) if err != nil { log.Fatalf(\u0026#34;cannot load plugin %v\u0026#34;, filename) } xmapf, err := p.Lookup(\u0026#34;Map\u0026#34;) if err != nil { log.Fatalf(\u0026#34;cannot find Map in %v\u0026#34;, filename) } mapf := xmapf.(func(string, string) []mr.KeyValue) xreducef, err := p.Lookup(\u0026#34;Reduce\u0026#34;) if err != nil { log.Fatalf(\u0026#34;cannot find Reduce in %v\u0026#34;, filename) } reducef := xreducef.(func(string, []string) string) return mapf, reducef } worker则是先load相应的map和reduce函数，然后调用Worker函数执行任务\n我们在这个lab中需要完成的就是src/mr/ 目录下的 coordinator.go，worker.go，rpc.go.\nGet Started! # 首先，我们需要确定coordinator需要记录哪些状态：\n// coordinator.go type TaskStatus int const ( Unassigned TaskStatus = iota Assigned Completed Failed ) type TaskInfo struct { TaskStatus TaskStatus // Status of task TaskFile string // File to process Timestamp time.Time // Assigned time } type Coordinator struct { MapNum int // Number of map tasks, in this lab it is the number of input files ReduceNum int // Number of reduce tasks, in this lab it is the number of nReduce MapTasks []TaskInfo // Map tasks record ReduceTasks []TaskInfo // Reduce tasks record AllMapTaskCompleted bool // Flag to indicate if all map tasks are completed AllReduceTaskCompleted bool // Flag to indicate if all reduce tasks are completed Mutex sync.Mutex // Mutex to protect shared data } 以及worker和coordinator之间传递的message的状态:\n// rpc.go type TaskType int const ( Map TaskType = iota Reduce Wait Exit ) type TaskCompleteStatus int const ( MapTaskSuccess TaskCompleteStatus = iota MapTaskFail ReduceTaskSuccess ReduceTaskFail ) type CoordinatorMsgReply struct { TaskID int // ID of task, when Map - map id and when Reduce - reduce id TaskType TaskType // Type of task - Map or Reduce File string // File to process ReduceNum int // Number of reduce tasks MapNum int // Number of map tasks } type WorkerMsgSend struct { TaskID int // ID of task TaskCompleteStatus TaskCompleteStatus // Status of task - completed or failed } 上图！ 可以看到待处理的文件首先根据map num分给worker进行处理，产生的intermediate文件根据reduce num进行split传给对应的worker进行处理(一般认为处理reduce的worker数量和reduce num相同).\n对于coordinator来说，它的工作就是：初始化-\u0026gt;等待worker请求任务的到来-\u0026gt;分配任务-\u0026gt;处理任务完成的消息-\u0026gt;所有任务执行完后结束job。其中关于rpc接收消息的部门和启动coordinator的部分在给的代码中已经写好。\n我们需要完成：初始化状态 + 处理worker两种类型的消息（请求任务/汇报任务完成）处理函数。\n// coordinator.go // 初始化过程中，主要关注一下 map\u0026amp;reduce tasks的设置 // PS：在这个项目中，map num定义为输入文件的数量，reduce num则由传入参数指定 func MakeCoordinator(files []string, nReduce int) *Coordinator { c := Coordinator{ MapNum: len(files), ReduceNum: nReduce, MapTasks: make([]TaskInfo, len(files)), ReduceTasks: make([]TaskInfo, nReduce), AllMapTaskCompleted: false, AllReduceTaskCompleted: false, Mutex: sync.Mutex{}, } // Initialize Coordinator for idx := range files { c.MapTasks[idx] = TaskInfo{ TaskFile: files[idx], TaskStatus: Unassigned, Timestamp: time.Now(), } } for idx := range c.ReduceTasks { c.ReduceTasks[idx] = TaskInfo{ TaskStatus: Unassigned, } } c.server() return \u0026amp;c } /* * RPC handler for worker to request task */ func (c *Coordinator) RequestTask(sendMsg *WorkerMsgSend, replyMsg *CoordinatorMsgReply) error { c.Mutex.Lock() defer c.Mutex.Unlock() // Assign Map Task if !c.AllMapTaskCompleted { // Count completed map tasks completedMapTaskNum := 0 for id, taskInfo := range c.MapTasks { // 判断一个任务可以被分配的条件：从未被分配 or 已经被分配但是没有在限定时间内处理完毕 or 任务执行失败 if taskInfo.TaskStatus == Unassigned || taskInfo.TaskStatus == Failed || (taskInfo.TaskStatus == Assigned \u0026amp;\u0026amp; time.Since(taskInfo.Timestamp) \u0026gt; 10*time.Second) { // Build reply message replyMsg.File = taskInfo.TaskFile replyMsg.MapNum = c.MapNum replyMsg.ReduceNum = c.ReduceNum replyMsg.TaskID = id replyMsg.TaskType = Map // Update map task record in coordinator c.MapTasks[id].TaskStatus = Assigned c.MapTasks[id].Timestamp = time.Now() return nil } else if taskInfo.TaskStatus == Completed { completedMapTaskNum++ } } if completedMapTaskNum == len(c.MapTasks) { c.AllMapTaskCompleted = true } else { // There are no available map tasks currently(some map tasks are still has Worker working on it) replyMsg.TaskType = Wait return nil } } // Assign Reduce Task if !c.AllReduceTaskCompleted { // Count completed reduce tasks completedReduceTaskNum := 0 for id, taskInfo := range c.ReduceTasks { if taskInfo.TaskStatus == Unassigned || taskInfo.TaskStatus == Failed || (taskInfo.TaskStatus == Assigned \u0026amp;\u0026amp; time.Since(taskInfo.Timestamp) \u0026gt; 10*time.Second) { // Build reply message replyMsg.MapNum = c.MapNum replyMsg.ReduceNum = c.ReduceNum replyMsg.TaskID = id replyMsg.TaskType = Reduce // Update reduce task record in coordinator c.ReduceTasks[id].TaskStatus = Assigned c.ReduceTasks[id].Timestamp = time.Now() return nil } else if taskInfo.TaskStatus == Completed { completedReduceTaskNum++ } } if completedReduceTaskNum == len(c.ReduceTasks) { c.AllReduceTaskCompleted = true } else { // There are no available reduce tasks currently(some reduce tasks are still has Worker working on it) replyMsg.TaskType = Wait return nil } } // All the tasks are completed replyMsg.TaskType = Exit return nil } /* * RPC handler for worker to report task completion */ func (c *Coordinator) ReportTask(sendMsg *WorkerMsgSend, replyMsg *CoordinatorMsgReply) error { c.Mutex.Lock() defer c.Mutex.Unlock() id := sendMsg.TaskID if sendMsg.TaskCompleteStatus == MapTaskSuccess { c.MapTasks[id].TaskStatus = Completed } else if sendMsg.TaskCompleteStatus == MapTaskFail { c.MapTasks[id].TaskStatus = Failed } else if sendMsg.TaskCompleteStatus == ReduceTaskSuccess { c.ReduceTasks[id].TaskStatus = Completed } else if sendMsg.TaskCompleteStatus == ReduceTaskFail { c.ReduceTasks[id].TaskStatus = Failed } return nil } 对于worker来说，与coordinator相对应的，它就需要执行map/reduce任务。 在Worker函数中，我们通过循环让worker处于一直执行任务的状态，直到所有的任务处理完成。\n// main/mrworker.go calls this function. func Worker(mapf func(string, string) []KeyValue, reducef func(string, []string) string) { for { sendMsg := WorkerMsgSend{} replyMsg := CoordinatorMsgReply{} call(\u0026#34;Coordinator.RequestTask\u0026#34;, \u0026amp;sendMsg, \u0026amp;replyMsg) switch replyMsg.TaskType { case Map: // Map Task MapTask(mapf, replyMsg.File, replyMsg.TaskID, replyMsg.ReduceNum) case Reduce: // Reduce Task ReduceTask(reducef, replyMsg.TaskID, replyMsg.MapNum) case Wait: time.Sleep(1 * time.Second) case Exit: os.Exit(0) default: time.Sleep(1 * time.Second) } } } 在执行任务函数的实现过程中，注意文件名生成的格式：\nintermediate file的文件名格式遵循：mr-{taskID}-{reduceIdx} (taskID stands for map task ID, ReduceIdx stands for the ID of reduce machine AKA reduce task ID) output file的文件名格式遵循：mr-{mapIdx}-{taskID} (mapIdx stands for index in map num, taskID stands for reduce task ID AKA reduce index) 这两个文件命名比较绕，容易搞糊\n// Map Task func MapTask(mapf func(string, string) []KeyValue, taskFile string, taskID int, reduceNum int) { // Open file file, err := os.Open(taskFile) if err != nil { log.Fatalf(\u0026#34;cannot open %v\u0026#34;, taskFile) return } // Read file content, err := io.ReadAll(file) if err != nil { log.Fatalf(\u0026#34;cannot read %v\u0026#34;, taskFile) return } file.Close() // Call mapf kva := mapf(taskFile, string(content)) // Create intermediate files intermediate := make([][]KeyValue, reduceNum) for i := 0; i \u0026lt; reduceNum; i++ { intermediate[i] = []KeyValue{} } // Partition intermediate data for _, kv := range kva { reduceIdx := ihash(kv.Key) % reduceNum intermediate[reduceIdx] = append(intermediate[reduceIdx], kv) } // Write intermediate data to files // File name format: // mr-{taskID}-{reduceIdx} (taskID stands for map task ID, ReduceIdx stands for the ID of reduce machine AKA reduce task ID) for i := 0; i \u0026lt; reduceNum; i++ { oname := fmt.Sprintf(\u0026#34;mr-%v-%v\u0026#34;, taskID, i) ofile, _ := os.Create(oname) enc := json.NewEncoder(ofile) for _, kv := range intermediate[i] { enc.Encode(kv) } ofile.Close() } // Report task completion sendMsg := WorkerMsgSend{ TaskID: taskID, TaskCompleteStatus: MapTaskSuccess, } replyMsg := CoordinatorMsgReply{} call(\u0026#34;Coordinator.ReportTask\u0026#34;, \u0026amp;sendMsg, \u0026amp;replyMsg) } func ReduceTask(reducef func(string, []string) string, taskID int, mapNum int) { // Load intermediate data - get all intermediate data from all map tasks which are hashed to this reduce task intermediate := []KeyValue{} for i := 0; i \u0026lt; mapNum; i++ { oname := fmt.Sprintf(\u0026#34;mr-%v-%v\u0026#34;, i, taskID) file, err := os.Open(oname) if err != nil { log.Fatalf(\u0026#34;cannot open %v\u0026#34;, oname) return } dec := json.NewDecoder(file) for { var kv KeyValue if err := dec.Decode(\u0026amp;kv); err != nil { break } intermediate = append(intermediate, kv) } file.Close() } // Sort intermediate data sort.Slice(intermediate, func(i, j int) bool { return intermediate[i].Key \u0026lt; intermediate[j].Key }) // Write to output file oname := fmt.Sprintf(\u0026#34;mr-out-%v\u0026#34;, taskID) ofile, err := os.Create(oname) if err != nil { log.Fatalf(\u0026#34;cannot create %v\u0026#34;, oname) return } // Call reducef for i := 0; i \u0026lt; len(intermediate); { j := i values := []string{} for j \u0026lt; len(intermediate) \u0026amp;\u0026amp; intermediate[j].Key == intermediate[i].Key { values = append(values, intermediate[j].Value) j++ } output := reducef(intermediate[i].Key, values) fmt.Fprintf(ofile, \u0026#34;%v %v\\n\u0026#34;, intermediate[i].Key, output) i = j } ofile.Close() // Report task completion sendMsg := WorkerMsgSend{ TaskID: taskID, TaskCompleteStatus: ReduceTaskSuccess, } replyMsg := CoordinatorMsgReply{} call(\u0026#34;Coordinator.ReportTask\u0026#34;, \u0026amp;sendMsg, \u0026amp;replyMsg) } Test and Grade # 执行测试： ","date":"15 August 2024","permalink":"/posts/mit6.5840/lab1/","section":"Posts","summary":"开一个新坑（我就是这种喜欢开很多坑然后三分钟热度的人😭\nFrom MIT 6.5840 / Spring 2024, Lab1 - MapReduce\nMapReduce Paper: http://research.google.com/archive/mapreduce-osdi04.pdf\nPre Handle # 在 src/main/ 目录下，已经给出了sequential版本的实现: mrsequential.go(只需要运行该文件就可以完成sequential mapreduce).\n在这个目录下还有两个文件 mrcoordinator.go \u0026amp; mrworker.go，它们会调用 src/mr/ 目录下的 coordinator.go和worker.go文件中的函数来完成分片模拟下的mapreduce的处理。\n我们可以观察一下mrcoordinator.go 和 mrworker.","title":"MIT 6.5840 Lab1"},{"content":"","date":"15 August 2024","permalink":"/series/mit6.5840/","section":"Series","summary":"","title":"MIT6.5840"},{"content":"","date":"15 August 2024","permalink":"/tags/project/","section":"Tags","summary":"","title":"project"},{"content":"","date":"15 August 2024","permalink":"/series/","section":"Series","summary":"","title":"Series"},{"content":"","date":"15 August 2024","permalink":"/tags/","section":"Tags","summary":"","title":"Tags"},{"content":" From CMU15-445 / Spring 2023, Project0 - C++ Primer\nBackground # 实现一个concurrent copy-on-write trie.\nTask 1: Copy-on-write Trie # 需要实现的函数有3个:\nGet(key) 从root开始向下遍历，直到找到 TrieNodeWithValue 类型节点. 注意 key 为空和 dynamic_cast 后为空的边界情况即可.\nPut(key, value) 在自上而下寻找插入位置时，会遇到当前到达节点已经为空而剩余key长度还大于1的情况，所以在这里使用递归比迭代处理更为方便.\nRemove(key) 与 Put 函数同理，递归处理在实现上更为方便.\n需要注意的是，在匹配到的 TrieNodeWithValue 节点没有 children_ 的情况下，需要删除当前节点返回 nullptr ，如果匹配到的节点仍然存在其他子节点，只需要将 TrieNodeWithValue 类型转换为 TrieNode ，并保留当前节点所有的子节点.\n同样的，在递归函数返回时，需要手动判断返回值是否为空，如果为空说明被调用子节点已被删除，需要手动更新当前节点的 children_ ，同时也要判断更新后子节点数量，以判断是否当前节点也需要删除.\nif (cur-\u0026gt;children_.count(c)) { auto copy_node = cur-\u0026gt;Clone(); auto ret_node = RemoveHelper(key.substr(1), cur-\u0026gt;children_.at(c)); if (ret_node == nullptr) { copy_node-\u0026gt;children_.erase(c); if(copy_node-\u0026gt;children_.empty() \u0026amp;\u0026amp; !copy_node-\u0026gt;is_value_node_){ return nullptr; } }else{ copy_node-\u0026gt;children_[c] = ret_node; } return copy_node; } // Typical Test TEST(TrieTest, RemoveFreeTest) { auto trie = Trie(); trie = trie.Put\u0026lt;uint32_t\u0026gt;(\u0026#34;test\u0026#34;, 2333); trie = trie.Put\u0026lt;uint32_t\u0026gt;(\u0026#34;te\u0026#34;, 23); trie = trie.Put\u0026lt;uint32_t\u0026gt;(\u0026#34;tes\u0026#34;, 233); trie = trie.Remove(\u0026#34;tes\u0026#34;); trie = trie.Remove(\u0026#34;test\u0026#34;); ASSERT_EQ(trie.GetRoot()-\u0026gt;children_.at(\u0026#39;t\u0026#39;)-\u0026gt;children_.at(\u0026#39;e\u0026#39;)-\u0026gt;children_.size(), 0); trie = trie.Remove(\u0026#34;te\u0026#34;); ASSERT_EQ(trie.GetRoot(), nullptr); } Task 2 # TrieStore.h 已经提供了两个锁：root_lock_ , write_lock_. 按照已经提供的伪代码的思路实现即可.\nTask 3 # Clion里打个断点就能解决的事情 😋\nTask 4 # 要求实现 upper 和 lower 的功能，同task2，伪代码写的很详细.\n","date":"17 April 2024","permalink":"/posts/cmu15-445/project0/","section":"Posts","summary":"From CMU15-445 / Spring 2023, Project0 - C++ Primer\nBackground # 实现一个concurrent copy-on-write trie.\nTask 1: Copy-on-write Trie # 需要实现的函数有3个:\nGet(key) 从root开始向下遍历，直到找到 TrieNodeWithValue 类型节点. 注意 key 为空和 dynamic_cast 后为空的边界情况即可.","title":"CMU 15-445 Project0"},{"content":"","date":"17 April 2024","permalink":"/series/cmu15-445/","section":"Series","summary":"","title":"CMU15-445"},{"content":"","date":"17 April 2024","permalink":"/tags/database/","section":"Tags","summary":"","title":"database"},{"content":" From CMU15-445 / Fall 2022, Homework1 - SQL\nQ1 # SELECT DISTINCT(language) FROM akas ORDER BY language LIMIT 10; Q2 # select primary_title, premiered, runtime_minutes||\u0026#34; (mins)\u0026#34; from titles where genres like \u0026#34;%Sci-Fi%\u0026#34; order by runtime_minutes desc limit 10; Q3 # select name, (died - born) as age from people where died is not NULL order by age desc limit 20; Q4 # select p.name, count(*) as num_appearances from people p join crew c on p.person_id = c.person_id group by p.name order by num_appearances desc limit 20; Q5 # 需要把 Integer 类型的 premiered 字段转化为字符串.\nselect cast(premiered/10*10 as text)||\u0026#39;s\u0026#39; as decade, round(avg(rating), 2) as avg_rating, max(rating) as top_rating, min(rating) as min_rating, count(*) as num_releases from titles join ratings on titles.title_id = ratings.title_id where titles.premiered is not NULL group by decade order by avg_rating desc, decade; Q6 # 使用CTE(Common Table Expressions)来获得临时表结果.\nwith cruise_movies as ( select crew.title_id from crew inner join people on crew.person_id = people.person_id where people.name like \u0026#34;%Cruise%\u0026#34; and people.born = 1962 ) select titles.primary_title as name, ratings.votes as votes from cruise_movies inner join ratings on cruise_movies.title_id = ratings.title_id inner join titles on cruise_movies.title_id = titles.title_id order by votes desc limit 10; Q7 # select count(title_id) from titles where premiered in ( select premiered from titles where primary_title = \u0026#34;Army of Thieves\u0026#34; ); Q8 # 注意因为 actor 和 actress 是属于不同的类型（可存在相同名字但分别是男/女演员的情况），所以需要查找的是具有唯一标识的 person_id.\nwith nk_movies as ( select distinct title_id from crew join people on crew.person_id = people.person_id where people.name = \u0026#39;Nicole Kidman\u0026#39; and people.born = 1967 and crew.category = \u0026#39;actress\u0026#39; ), nk_colleagues as ( select distinct person_id from crew where category in (\u0026#39;actor\u0026#39;, \u0026#39;actress\u0026#39;) and title_id in nk_movies ) select name from people join nk_colleagues on people.person_id = nk_colleagues.person_id order by name; Q9 # with people_1955 as ( select distinct person_id from people where people.born = 1955 ), movies_1955 as ( select person_id, title_id from crew where person_id in people_1955 ), ratings_1955 as ( select person_id, titles.title_id, rating from movies_1955 join titles on movies_1955.title_id = titles.title_id join ratings on movies_1955.title_id = ratings.title_id where titles.type = \u0026#39;movie\u0026#39; ), processed_ratings as ( select people.name, round(avg(rating), 2) as avg_rating from people join ratings_1955 on people.person_id = ratings_1955.person_id group by people.name ), quantiles as ( select *, ntile(10) over (order by avg_rating asc) as rating_quartile from processed_ratings ) select name, avg_rating from quantiles where rating_quartile = 9 order by avg_rating desc, name asc; Q10 # Recursive CTE.\nwith p as ( select distinct akas.title as dubbed from titles inner join akas on titles.title_id = akas.title_id where titles.primary_title = \u0026#34;House of the Dragon\u0026#34; AND titles.type = \u0026#39;tvSeries\u0026#39; order by akas.title ), c as ( select row_number() over (order by p.dubbed asc) as seqnum, p.dubbed as dubbed from p ), flattened as ( select seqnum, dubbed from c where seqnum = 1 union all select c.seqnum, f.dubbed || \u0026#39;, \u0026#39; || c.dubbed from c join flattened f on c.seqnum = f.seqnum + 1 ) select dubbed from flattened order by seqnum desc limit 1; ","date":"5 April 2024","permalink":"/posts/cmu15-445/hw1/","section":"Posts","summary":"From CMU15-445 / Fall 2022, Homework1 - SQL\nQ1 # SELECT DISTINCT(language) FROM akas ORDER BY language LIMIT 10; Q2 # select primary_title, premiered, runtime_minutes||\u0026#34; (mins)\u0026#34; from titles where genres like \u0026#34;%Sci-Fi%\u0026#34; order by runtime_minutes desc limit 10; Q3 # select name, (died - born) as age from people where died is not NULL order by age desc limit 20; Q4 # select p.","title":"CMU 15-445 Homework1"},{"content":"","date":"5 April 2024","permalink":"/tags/homework/","section":"Tags","summary":"","title":"homework"},{"content":"","date":"13 June 2022","permalink":"/posts/","section":"Posts","summary":"","title":"Posts"},{"content":" Blowfish is packed with tons of features. The original aim of Blowfish was to develop a theme that was simple and lightweight. The theme is a fork of Congo and expands its initial vision.\nTailwind CSS 3.0 # Tailwind CSS is at the heart of Blowfish and this release contains the very latest Tailwind CSS version 3. It brings with it performance optimisations and support for some great new CSS features.\nMultilingual support # A highly requested feature, Blowfish is now multilingual! If you publish your content in multiple languages, the site will be built with all the translations available.\n🇬🇧 🇩🇪 🇫🇷 🇪🇸 🇨🇳 🇧🇷 🇹🇷 🇧🇩 Thanks to submissions from the community, Blowfish has already been translated into eight languages with more to be added over time. By the way, pull requests for new languages are always welcome!\nRTL language support # One of the benefits of the new Tailwind and Multilingual features is the ability to add RTL language support. When enabled, the entire site will reflow content from right-to-left. Every element in the theme has been restyled to ensure it looks great in this mode which aids authors who wish to generate content in RTL languages.\nRTL is controlled on a per-language basis so you can mix and match both RTL and LTR content in your projects and the theme will respond accordingly.\nAutomatic image resizing # A big change in Blowfish 2.0 is the addition of automatic image resizing. Using the power of Hugo Pipes, images in Markdown content are now automatically scaled to different output sizes. These are then presented using HTML srcset attributes enabling optimised file sizes to be served to your site visitors.\n\u0026lt;!-- Markdown: ![My image](image.jpg) --\u0026gt; \u0026lt;img srcset=\u0026#34; /image_320x0_resize_q75_box.jpg 320w, /image_635x0_resize_q75_box.jpg 635w, /image_1024x0_resize_q75_box.jpg 1024w, /image_1270x0_resize_q75_box.jpg 2x\u0026#34; src=\u0026#34;/image_635x0_resize_q75_box.jpg\u0026#34; alt=\u0026#34;My image\u0026#34; /\u0026gt; Best of all there\u0026rsquo;s nothing you need to change! Simply insert standard Markdown image syntax and let the theme do the rest. If you want a little more control, the figure shortcode has been completely rewritten to provide the same resizing benefits.\nSite search # Powered by Fuse.js, site search allows visitors to quickly and easily find your content. All searches are performed client-side meaning there\u0026rsquo;s nothing to configure on the server and queries are performed super fast. Simply enable the feature in your site configuration and you\u0026rsquo;re all set. Oh, and it also supports full keyboard navigation!\nTables of contents # A highly requested feature, Blowfish now supports tables of contents on article pages. You can see it in action on this page. The contents are fully responsive and will adjust to take advantage of the space available at different screen resolutions.\nAvailable on a global or per article basis, the table of contents can be fully customised using standard Hugo configuration values, allowing you to adjust the behaviour to suit your project.\nAccessibility improvements # From adding ARIA descriptions to more items or simply adjusting the contrast of certain text elements, this release is the most accessible yet.\nVersion 2 also introduces \u0026ldquo;skip to content\u0026rdquo; and \u0026ldquo;scroll to top\u0026rdquo; links that enable quick navigation. There\u0026rsquo;s also keyboard shortcuts for enabling items like search without reaching for the mouse.\nThe new image resizing features also provide full control over alt and title elements enabling an accessible experience for all visitors.\nA whole lot more # There\u0026rsquo;s countless other features to explore. From being able to display taxonomies on articles and list pages, to using the new headline author parameter to customise your homepage. There\u0026rsquo;s also improved JSON-LD structured data which further optimises SEO performance.\n","date":"19 January 2022","permalink":"/posts/welcome/","section":"Posts","summary":"Blowfish is packed with tons of features. The original aim of Blowfish was to develop a theme that was simple and lightweight. The theme is a fork of Congo and expands its initial vision.","title":"About Blowfish"},{"content":"","date":"19 January 2022","permalink":"/tags/new/","section":"Tags","summary":"","title":"new"},{"content":"This is the advanced tag. Just like other listing pages in Blowfish, you can add custom content to individual taxonomy terms and it will be displayed at the top of the term listing. 🚀\nYou can also use these content pages to define Hugo metadata like titles and descriptions that will be used for SEO and other purposes.\n","date":"1 January 0001","permalink":"/tags/advanced/","section":"Tags","summary":"This is the advanced tag. Just like other listing pages in Blowfish, you can add custom content to individual taxonomy terms and it will be displayed at the top of the term listing.","title":"Advanced"},{"content":"","date":"1 January 0001","permalink":"/authors/","section":"Authors","summary":"","title":"Authors"},{"content":"","date":"1 January 0001","permalink":"/categories/","section":"Categories","summary":"","title":"Categories"}]